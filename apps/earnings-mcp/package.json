{
  "name": "@cfp/earnings-mcp",
  "private": true,
  "version": "0.1.0",
  "description": "Earnings-potential research MCP connector — UW public API evidence tools for claude.ai (Streamable HTTP) and Claude Code (stdio). Standalone: NOT in the pnpm workspace; npm install here.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "http": "PORT=8787 node server.mjs",
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
