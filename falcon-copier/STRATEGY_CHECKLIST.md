# The Reversal Checklist — our defined edge (the thing to execute with discipline)

Per Glitch/Falcon: the edge is NOT a discovered signal — it's a **pre-defined checklist executed with
ruthless selectivity and identical sizing.** "It takes only the setups that mark off every single
required condition. No exceptions." Falcon's take-every-card book = −19%; the edge is the SELECTION.

Our five failed sims took 4–6 loose reversals/day (any pika, any extreme) and were curve-fit. This is
the correction: **ALL conditions must be true, or NO TRADE.** Expect FEW trades/day. Forward-validate;
do NOT fit to the 9 replay days.

## ENTRY — every box must be checked (100% or skip)
A short (BEAR) example; a long (BULL) is the mirror.
1. **Regime:** net 0DTE gamma > 0 (positive gamma = mean-reversion regime). [have]
2. **At the WALL:** spot within ±$5 of the **call wall** = the single largest pika above spot (for a
   short). Not a lesser node. [have]
3. **Wall is real dealer structure:** the wall has NOT been tested ≥2× already this session
   (repeatedly-tested = customer-held → breaks). [have]
4. **Deceleration:** the move INTO the wall is slowing (last-3-bar velocity into the wall < prior
   velocity) — not accelerating through it. [have, from spot path]
5. **Vanna aligned:** near-spot 0DTE vanna supports down (vanna-velocity ≤ 0 for a short). [have — v0, UNUSED before]
6. **No vol shock:** VIX not up >X% intraday / no spike. [NEED: VIX 1-min]
7. **Rejection on volume:** the rejection bar prints on above-average volume (real, not a wick). [NEED: underlying volume]
8. **Clear path & R:R ≥ 3:1:** an air pocket (no opposing wall) between spot and the target (put wall /
   king), and the target is ≥ 3× the stop distance. [have]
9. **Window:** before 15:15 ET. [have]

## EXECUTION (non-negotiable, from the Commandments)
- **Identical size every trade.** Sizing is not a mood.
- **Stop pre-defined:** exit if spot breaks back through the wall (thesis dead). Decided before entry.
- **Target:** the opposite wall / the king (mean-reversion to the pin).
- **Log every trade** + every skipped-because-condition-N-failed.

## THE NUMBERS WE MUST STATE (or we don't have a system)
Track forward: **win rate, profit factor, expectancy ($/trade), avg win, avg loss, # trades/day.**
Target = a modest, repeatable edge (base hits), NOT home runs. Consistency compounds; one emotional/
oversized blow-up resets the exponent.

## HONEST GAP
We cannot check boxes 6 (VIX) and 7 (volume) from the current replay — and the research says those two
are the KEY hold-vs-break confirmers. So the checklist is INCOMPLETE until we pull VIX + underlying
volume. A partial checklist is still less selective than Falcon's full one → expect it to underperform
Falcon until complete.

## PROCESS (Glitch's order — do NOT skip)
1. DEFINE the checklist (this doc).  2. Pull the missing inputs (VIX, volume).  3. Make the sim require
ALL boxes (ultra-selective).  4. SANITY-check on the 9 days (not validation — just "does it take few,
sane trades?").  5. **FORWARD-validate** with tracked stats before trusting or sizing up.
